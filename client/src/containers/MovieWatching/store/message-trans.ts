/*
 * MovieWatching  Messages
 *
 * This contains all the text for the MovieWatching component.
 */

import { defineMessages } from "react-intl";

export const scope = "MovieWatching";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
