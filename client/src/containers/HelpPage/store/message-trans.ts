/*
 * HelpPage  Messages
 *
 * This contains all the text for the HelpPage component.
 */

import { defineMessages } from "react-intl";

export const scope = "HelpPage";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
