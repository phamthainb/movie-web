/*
 * SearchMovie  Messages
 *
 * This contains all the text for the SearchMovie component.
 */

import { defineMessages } from "react-intl";

export const scope = "SearchMovie";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
